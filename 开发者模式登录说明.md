# 开发者模式登录说明

## 问题原因

小程序有两个独立的登录流程：

1. **开发者模式登录** (`/api/dev/login`)
   - 使用临时 userId（如 `dev_user_1770001296961`）
   - 用于快速测试，无需真实微信登录

2. **正常微信登录** (`/api/wechat/login`)
   - 使用 UUID 格式的 userId（如 `ecd2f3a4-199e-4d8c-ac45-08a2d0cd7e53`）
   - 小程序启动时自动执行

之前的问题是：开发者登录后，小程序会自动执行微信登录，导致 userId 被覆盖。

## 解决方案

### 1. 后端配置

已修改 `backend/.env`：
- ✅ 启用开发者模式：`DEV_MODE=true`
- ✅ 使用本地数据库：`USE_REMOTE_DB=false`
- ✅ 注释 CloudBase 配置以避免使用 RDB 模式

### 2. 小程序配置

已修改以下文件：

#### miniprogram/utils/cloudbase-request.js
```javascript
const USE_LOCAL_SERVER = true;  // 连接本地后端
const LOCAL_SERVER_URL = 'http://localhost:3000';
```

#### miniprogram/app.js
- `autoLogin()` 方法中添加开发者模式检查
- 如果开发者模式已激活，跳过自动微信登录

#### miniprogram/utils/devMode.js
- `activateDevMode()` 保存开发者模式状态到本地存储
- `initDevMode()` 从本地存储恢复开发者模式状态
- `disableDevMode()` 清除本地存储的开发者模式状态

## 使用方法

### 1. 启动本地后端

```bash
# 启动 Docker 和数据库
docker-compose up -d

# 启动后端（已在运行）
cd backend && pnpm run dev
```

### 2. 配置小程序

确保 `miniprogram/utils/cloudbase-request.js` 中：
```javascript
const USE_LOCAL_SERVER = true;  // ✅ 必须为 true
const LOCAL_SERVER_URL = 'http://localhost:3000';  // ✅ 端口要匹配
```

### 3. 使用开发者模式

1. 在小程序中快速点击状态栏 5 次激活开发者模式
2. 打开开发者面板，输入 userId 或留空自动生成
3. 点击"快速登录"
4. 此时不会触发自动微信登录，userId 保持不变

## 测试

### 本地后端测试

```bash
# 测试快速登录
curl 'http://localhost:3000/api/dev/login' \
  -H 'Content-Type: application/json' \
  -d '{"userId":"111"}'

# 返回
{
  "success": true,
  "message": "登录成功",
  "data": {
    "userId": "111",
    "usageCount": 3,
    "hasEverPaid": false,
    "paymentStatus": "free",
    "createdAt": "2026-02-02T02:43:49.000Z"
  }
}
```

### 小程序测试

1. 打开微信开发者工具
2. 确保小程序连接到本地后端（检查控制台日志）
3. 激活开发者模式（快速点击状态栏 5 次）
4. 使用开发者面板登录
5. 调用开发者 API（如设置使用次数）

## 常见问题

### Q: 小程序报错"开发者模式未启用"

**原因**：小程序正在连接云托管后端，而不是本地后端。

**解决**：
1. 检查 `miniprogram/utils/cloudbase-request.js`
2. 确保 `USE_LOCAL_SERVER = true`
3. 确保 `LOCAL_SERVER_URL = 'http://localhost:3000'`（端口要匹配）
4. 重新编译小程序

### Q: 小程序无法连接本地后端

**原因**：端口不匹配或后端未启动。

**解决**：
1. 确认后端正在运行：`curl http://localhost:3000/health`
2. 检查端口是否匹配（后端 `.env` 中的 `PORT` 和小程序中的 `LOCAL_SERVER_URL`）
3. 在微信开发者工具中启用"不校验合法域名"

### Q: 开发者模式激活后仍然执行微信登录

**原因**：开发者模式状态未正确保存或恢复。

**解决**：
1. 检查 `miniprogram/utils/devMode.js` 中的 `activateDevMode()` 和 `initDevMode()`
2. 确保本地存储中有 `devModeActive` 键
3. 重启小程序

## 环境配置总结

### backend/.env

```env
# 开发者模式
NODE_ENV=development
DEV_MODE=true

# 本地数据库
USE_REMOTE_DB=false
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=ai_family_photo

# CloudBase 配置（开发环境下注释）
# CLOUDBASE_ENV=test-1g71tc7eb37627e2
# TENCENTCLOUD_SECRETID=...
# TENCENTCLOUD_SECRETKEY=...
```

### miniprogram/utils/cloudbase-request.js

```javascript
const USE_LOCAL_SERVER = true;  // ✅ 本地开发必须为 true
const LOCAL_SERVER_URL = 'http://localhost:3000';  // ✅ 端口要匹配
```

