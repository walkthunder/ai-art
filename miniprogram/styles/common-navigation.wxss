/**
 * 通用导航栏样式
 * 使用 JS 动态计算的高度，不依赖 CSS env() 变量
 */

/* 页面容器 */
.page-with-nav {
  position: relative;
  min-height: 100vh;
  width: 100%;
}

/* 内容容器 - 为导航栏留出空间 */
.nav-content {
  position: relative;
  z-index: 10;
  width: 100%;
  box-sizing: border-box;
}

/* 顶部导航栏 */
.nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  width: 100%;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  background: #d4302b;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.15);
  box-sizing: border-box;
}

.nav-left {
  width: 136rpx;
  padding-left: 32rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: flex-start;
  gap: 0;
  flex-shrink: 0;
}

.back-btn {
  width: 72rpx;
  height: 72rpx;
  background: transparent;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.back-icon {
  font-size: 64rpx;
  color: #FFD700;
  font-weight: bold;
  line-height: 1;
}

.nav-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #FFD700;
  flex: 1;
  text-align: center;
  padding: 0 16rpx;
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  width: auto;
  max-width: 60%;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.nav-right {
  width: 136rpx;
  padding-right: 32rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: flex-end;
}

/* elder-mode-toggle 固定位置样式 */
.elder-mode-toggle-fixed {
  position: fixed;
  left: 32rpx;
  z-index: 9999;
}

/* ========================================
   大字模式适配
   ======================================== */

.elder-mode .back-btn {
  width: 88rpx;
  height: 88rpx;
}

.elder-mode .back-icon {
  font-size: 80rpx;
}

.elder-mode .nav-title {
  font-size: 42rpx;
}

/* ========================================
   富贵变身模式 (Transform) 导航栏覆盖
   所有 Transform 相关页面使用金色主题
   ======================================== */

.transform-launch-page .nav-bar,
.transform-upload-page .nav-bar,
.transform-generating-page .nav-bar,
.transform-result-page .nav-bar,
.transform-result-selector-page .nav-bar,
.transform-template-page .nav-bar,
.transform-history-page .nav-bar {
  background: #d4af37;
}

.transform-launch-page .back-icon,
.transform-upload-page .back-icon,
.transform-generating-page .back-icon,
.transform-result-page .back-icon,
.transform-result-selector-page .back-icon,
.transform-template-page .back-icon,
.transform-history-page .back-icon {
  color: #8B4513;
}

.transform-launch-page .nav-title,
.transform-upload-page .nav-title,
.transform-generating-page .nav-title,
.transform-result-page .nav-title,
.transform-result-selector-page .nav-title,
.transform-template-page .nav-title,
.transform-history-page .nav-title {
  color: #8B4513;
  text-align: center;
}

/* ========================================
   邀请页面导航栏覆盖
   ======================================== */

.invite-page .nav-bar {
  background: #D4AF37;
}

.invite-page .back-icon {
  color: #DC143C;
}

.invite-page .nav-title {
  color: #8B4513;
}

/* ========================================
   贺卡编辑页面导航栏覆盖
   ======================================== */

.card-editor-page .nav-bar {
  background: #8B0000;
}

.card-editor-page .back-icon {
  color: #FFD700;
}

.card-editor-page .nav-title {
  color: #FFD700;
}
