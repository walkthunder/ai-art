# AI Prompt 配置化管理指南

## 功能概述

所有 AI 调用接口的 prompt 已实现数据库配置化管理，支持通过管理后台动态调整，无需修改代码和重启服务。

## 核心特性

- ✅ 支持三种模式：时空拼图（puzzle）、富贵变身（transform）、财神变身（caishen）
- ✅ 数据库存储，实时生效
- ✅ 管理后台可视化编辑
- ✅ 代码降级兼容（数据库失败时使用代码配置）

## 使用方法

### 1. 访问管理后台

```
URL: http://localhost:5173 (开发环境)
路径: 模板管理
```

### 2. 编辑 Prompt

1. 在模板列表中找到需要优化的模板
2. 点击"编辑"按钮
3. 修改"AI提示词"字段
4. 保存后立即生效（无需重启）

### 3. 测试效果

1. 在小程序或前端测试生成功能
2. 根据生成效果继续调整 prompt
3. 重复优化直到满意

## 数据库结构

### templates 表字段

| 字段 | 说明 | 示例 |
|------|------|------|
| mode | 模式类型 | caishen |
| code | 模板代码 | caishen-default |
| name | 模板名称 | 财神发钱 |
| image_url | 预览图 | https://... |
| prompt | AI提示词 | 将用户上传的... |
| duration | 视频时长（秒） | 5 |
| status | 状态 | active/inactive |

## 技术实现

### 后端服务

- `backend/services/templateDbService.js` - 数据库读取服务
- `backend/config/templates.js` - 配置文件（支持降级）
- `backend/routes/adminTemplateRoutes.js` - 管理接口

### 前端页面

- `admin/src/pages/Templates/index.tsx` - 模板管理页面
- `admin/src/services/template.ts` - API 服务

### 数据库迁移

- `backend/db/migrations/026_add_caishen_templates.sql`

## 注意事项

1. Prompt 修改后立即影响所有新请求
2. 建议先在测试环境验证效果
3. 重要修改建议做好备份
4. duration 字段仅用于财神变身模式

## 故障排查

### 问题：修改后不生效

**解决方案：**
1. 检查模板状态是否为 active
2. 确认数据库连接正常
3. 查看后端日志是否有错误

### 问题：数据库读取失败

**解决方案：**
系统会自动降级到代码配置，不影响功能使用。检查数据库连接配置。

## 最佳实践

### Prompt 优化建议

1. **明确任务目标**：开头直接说明要做什么
2. **结构化描述**：使用【】分类不同要求
3. **强调关键点**：重要要求放在前面
4. **避免冗余**：去掉不必要的重复描述
5. **测试验证**：每次修改后测试效果

### 财神变身 Prompt 优化经验

**问题：视频分段，人物不一致**

**解决方案：**
- 强调"单镜头连贯"
- 简化动作描述
- 避免过度强调面部替换（可能适得其反）

**当前最佳实践：**
```
将用户上传的人物照片转换为财神爷发钱的视频，
生成一个连贯流畅的单镜头场景。

【人物要求】
1. 将用户照片中的人脸特征完整应用到财神爷身上
2. 保持用户的五官、脸型、肤色等面部特征
...

【动作内容】
财神爷手持金元宝向四周撒金币，动作连贯自然。
整个视频是一个完整的连续镜头，不切换场景。
```

## 部署说明

### 数据库迁移

生产环境部署时需要执行迁移：

```bash
node backend/scripts/run-migrations.js
```

### 初始数据

迁移文件会自动插入默认模板数据，包括：
- 2个财神变身模板
- 3个富贵变身模板  
- 2个时空拼图模板

## 总结

通过配置化管理，AI prompt 的优化变得更加灵活和高效，无需修改代码即可快速迭代优化生成效果。
