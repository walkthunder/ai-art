# 任务列表

- 添加水印功能，应该要添加上这个小程序的二维码，当前小程序的名字是WhisperAI
- 在每个模式的结果页面，以及每个模式的首页，应该明显有入口，支持小程序充值。
- 在结果页面，当用户选择了免费版本，保存图片之后，那个保存的button应该切换为高清下载，点击弹出充值弹窗。因为可能用户发现免费版本不够高清，还有水印，所以想要付费升级了。
- 将所有用到小程序名字的地方，可以都放在一个配置的地方，统一修改小程序名字，包括页面上，还有加水印的地方，还有隐私页面等里面，方便修改，当前小程序的名字是WhisperAI，后面可能会改成：团圆照相馆，但是还没有通过审核